# Torch/CUDA come from the base image (pytorch/pytorch CUDA runtime).
# Pin coqui-tts + transformers as a COHERENT PAIR. The latest coqui-tts chases a
# bleeding-edge transformers whose API keeps shifting (no single version has all the
# symbols it imports) — so pin an older stable coqui-tts and the transformers it was
# built against. numpy<2 is required by this era's librosa/numba stack.
coqui-tts==0.24.3
transformers==4.46.2
numpy<2
pydub>=0.25
pymupdf>=1.24
fastapi>=0.110
uvicorn[standard]>=0.27
requests>=2.31
